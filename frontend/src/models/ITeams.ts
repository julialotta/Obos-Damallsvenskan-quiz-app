export interface ITeams {
  id: number;
  team: string;
  link: string;
}

export interface IGames {
  id: number;
  points: number;
}
export interface IResult {
  id: number;
  team: string;
  points: number;
}
